apiVersion: v1
kind: Service
metadata:
  name: {{ include "cyberpower-pdu-mqtt-bridge.fullname" . }}
  labels:
    {{- include "cyberpower-pdu-mqtt-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cyberpower-pdu-mqtt-bridge.selectorLabels" . | nindent 4 }}
