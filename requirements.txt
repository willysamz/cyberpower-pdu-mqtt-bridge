# FastAPI and server
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# SNMP (maintained fork; upstream pysnmp is unmaintained).
# Pin pyasn1<0.6.0 because pysnmp-lextudio 6.1.2 imports
# pyasn1.compat.octets, which was removed in pyasn1 0.6.x.
pysnmp-lextudio==6.1.2
pyasn1==0.5.1

# MQTT (asyncio wrapper over paho-mqtt)
aiomqtt==2.0.1

# Utilities
pyyaml==6.0.1
structlog==24.1.0

# Development
ruff==0.2.1
mypy==1.8.0
pytest==7.4.4
pytest-asyncio==0.23.4
httpx==0.26.0
